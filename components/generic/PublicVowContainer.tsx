import { PublicVowResponse } from "@/types/PublicVowResponse";

export const PublicVowContainer = ({
  vowId,
  statusFormatted,
  title,
  commentCount,
  viewCount,
  createdAt,
}: PublicVowResponse) => {
  return <div></div>;
};
